<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorBlack</item>
    </style>

    <style name="PostsTheme" parent="Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/PostsActionBarTheme</item>
    </style>

    <style name="PostsActionBarTheme" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/colorWhite</item>
        <item name="titleTextStyle">@style/PostsBarTitle</item>
        <item name="icon">@drawable/nav_logo_whiteout</item>
    </style>

    <style name="PostsBarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
    </style>

</resources>